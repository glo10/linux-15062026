# Atelier 2.2 : gestion des paquetages, bibliothèques partagées et archives

## Objectifs

- [x] Maitrisez la gestion des paquets avec apt, dkpg, dnf, rpm
- [x] Maitrisez la gestion des bibliothèques partagées (.so)
- [x] Maitrisez l'installation des logiciels via une archive

---

## Partie I : gestionnaire de paquet `dnf`

1. Créez et démarrez une nouvelle machine virtuelle Fedora en mode texte et minimale avec des caractéristiques similaire à la VM debian 13 cf [atelier 1.1](1.1.md)
2. Installez les paquets nano et docker via le gestionnaire de paquets `dnf`
3. Installez le paquet okular via `dnf`, observez le retour de votre commande d'installation, que pouvez-vous en déduire ?
4. Exécutez la commande suivante ***sudo docker ps***, que pouvez-vous en déduire du résultat ?
5. Regardez l'état du service docker à l'aide de la commande `systemctl`, s'il n'est pas démarré, lancez le.

---

## Partie II : gestionnaires de paquetage apt et dkpg

6. Allumez la VM Debian
7. Installez `nano` et `docker.io` via `apt`
8. Utilisez la commande `dpkg -L` nano pour localiser le fichier exécutable de `nano` (dans `/bin` ou `/usr/bin`).
9. Exécutez la commande ldd sur l'exécutable de `nano`. Combien de bibliothèques partagées (.so) ce programme utilise-t-il pour fonctionner ?

---

## Partie III : gestion d'une archive

10. Toujours depuis la distribution Debian, [téléchargez l'archive de Node](https://nodejs.org/dist/v26.3.0/node-v26.3.0-linux-x64.tar.xz) au format `.tar.xz` via la commande wget dans le dossier ***`/tmp`***.
```bash
wget -P /tmp https://nodejs.org/dist/v26.3.0/node-v26.3.0-linux-x64.tar.xz
```
11. Extrayez l'archive dans `/tmp` à l'aide de la commande suivante 
```bash
tar -xvf /tmp/node-v26.3.0-linux-x64.tar.xz -C /tmp
# ou bien en précisant qu'il s'agit d'un XZ
tar -xJvf /tmp/node-v26.3.0-linux-x64.tar.xz -C /tmp
```
*** ***
12. Testez son exécution à l'aide de la commande
```bash
/tmp/node-v26.3.0-linux-x64/bin/node --version # retour attendu v26.3.0
```

---

## Bonus

13. Téléchargez `VSCode` avec `wget` depuis ce lien [https://code.visualstudio.com/download#download-alt-linux64_deb](https://code.visualstudio.com/download#download-alt-linux64_deb) depuis le site de l'éditeur dans le dossier `/tmp`
14. Essayez d'installer VSCode avec ***`dkpg -i`***
15. Comment pouvez-vous résoudre l'erreur obtenu lors de la tentative d'installation
16. Ajoutez ***node*** dans les variables d'environnement pour l'exécuter depuis n'importe quel dossier.
- Indice, il faut modifier la variable d'environnement PATH